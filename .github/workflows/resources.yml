name: schedule@v1

on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  run-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a script 
        run: echo "This runs every minute"